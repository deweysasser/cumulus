# ToDos and random thoughts

# TODO

* if we're filtering, only display fields with values for the selected lines (rather than all fields
  from all lines)
* should we automatically show fields involved in matching?